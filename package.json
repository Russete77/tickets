{
  "name": "ticketeria",
  "private": true,
  "workspaces": [
    "ticketeria-types",
    "ticketeria-api",
    "ticketeria-web",
    "ticketeria-admin"
  ],
  "scripts": {
    "dev:api": "npm run dev -w ticketeria-api",
    "dev:web": "npm run dev -w ticketeria-web",
    "dev:admin": "npm run dev -w ticketeria-admin",
    "build": "npm run build -w ticketeria-types && npm run build -w ticketeria-api && npm run build -w ticketeria-web && npm run build -w ticketeria-admin",
    "build:types": "npm run build -w ticketeria-types",
    "build:api": "npm run build -w ticketeria-api",
    "build:web": "npm run build -w ticketeria-web",
    "build:admin": "npm run build -w ticketeria-admin",
    "test": "npm run test -w ticketeria-api",
    "test:api": "npm run test -w ticketeria-api",
    "lint": "npm run lint -w ticketeria-api && npm run lint -w ticketeria-web && npm run lint -w ticketeria-admin",
    "typecheck": "npm run typecheck -w ticketeria-api && npm run typecheck -w ticketeria-web && npm run typecheck -w ticketeria-admin",
    "db:generate": "npm run db:generate -w ticketeria-api",
    "db:migrate": "npm run db:migrate -w ticketeria-api",
    "db:seed": "npm run db:seed -w ticketeria-api"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
